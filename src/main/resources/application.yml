
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jpa_database
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
